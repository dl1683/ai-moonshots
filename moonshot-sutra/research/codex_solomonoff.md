1. No pivot: keep `compression-first`, but make stages justify distinct compression operators; merge any stage that doesn’t reduce description length.
2. Better Solomonoff approximation = lower `BPB` at matched params/FLOPs via adaptive segmentation, stronger `Stage 2→4→5` routing, sparse retrieval, and occasional attention refresh.
3. Yes: formalize Sutra as an adaptive operator program; measure MDL of the learned operation traces.
4. Message passing helps only if it sends constraints/beliefs, not generic features; test `BP-skeleton + learned correction`.
5. Revive `adaptive operation sequencing` and `belief-structured passing`; keep ensemble/boosting-round ideas killed.
6. Build path: run matched ablations; ship only mechanisms that improve both `BPB` and reasoning.